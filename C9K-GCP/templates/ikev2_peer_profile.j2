crypto ikev2 profile ikev2-peer-profile-{{tunnel_name}}
    match identity remote address {{ike_peer_profile.remote_ip}} 255.255.255.255
    authentication remote pre-share
    authentication local pre-share
    keyring local ikev2-keyring-{{tunnel_name}}
